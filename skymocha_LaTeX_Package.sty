% Literally just for my class notes & homework
% 
% (c) Skye Kychenthal
% 

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{skymocha_LaTeX_Package}[Skye Kychenthal LaTeX Package for Math]

\RequirePackage{amsmath}
\RequirePackage{amssymb}

\newcommand{\header}[6]{

    \begin{center}
        \Large{ \textbf{#1} #2}\\
        \vspace*{0.175in}
        \textbf{#3}\\
        \vspace*{0.125in}
        Week #4: #5 $\to$ #6\\
        \vspace*{0.065in}
        \small Using Skye Kychenthal's \LaTeX Package
        \\
    \end{center}

}

\newcommand{\picewise}[3]{
    \begin{equation}
        #1 =
        \lbrace
        \begin{array}{lr}
            #2 \\
            #3
        \end{array}
    \end{equation}
}

% X is an element of all real numbers
\newcommand{\xer}{x \in \mathbb{R} :}

% Formal definition of a limit
\newcommand{\limdef}[3]{\forall \epsilon > 0 \hspace*{0.125in} 
            \exists \delta > 0 \hspace*{0.0625in} \textrm{  such that  }
            \hspace*{0.0625in} \textrm{if  } 0 < \mid x - #1 \mid < \delta, 
            \hspace*{0.125} \textrm{then}
            \hspace*{0.625} \mid #2 - #3 \mid < \epsilon }
% Adds comments to proofs
\newcommand{\comment}[1]{ \hspace*{0.125in}\to\hspace*{0.175in} \textrm{#1} }
% Shorthand for limits
\newcommand{\limlim}[3]{ \lim_{ #1 \to #2 } #3 }

\newcommand{\meow}{nya}

\let\myDate\date

\endinput
%%
%% End of file skymocha_LaTeX_Package.sty'.